services:
  - type: web
    name: fastapi-backend
    env: python
    repo: https://github.com/godofwar64/restaurant-api.git
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    root: backend
